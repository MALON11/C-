#include <stdio.h>
int main()

{
float base;
float height;
float area;

printf(*enter the base*);
scanf("base");

printf(*enter the height*);
scanf("height");

area=1/2*base*height;

printf("the area is%f",area);

return 0;

}